// Единый тумблер режима «один Instagram-аккаунт».
// Читают и панель (Vite-бандл), и Netlify Functions (esbuild) — чтобы правило
// не разъезжалось между фронтом и бэком.
//
// true  — в проекте ровно один бренд-аккаунт и ровно один подключённый Instagram:
//         панель прячет переключатель брендов, не даёт создать второй бренд,
//         а connect-instagram отказывается подключать второй профиль.
// false — обратно multi-tenant: несколько брендов у одного владельца.
//         Схема Firestore, правила и функции это уже умеют — менять их не нужно.
export const SINGLE_ACCOUNT_MODE = true
